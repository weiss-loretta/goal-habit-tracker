# DESIGN_WORKFLOW.md

## Phase 3.37 驗證重點
- 五個主要頁面需通過 320 × 568、375 × 667、390 × 844、430 × 932 viewport matrix，且不可出現水平 overflow。
- 長名稱在極窄畫面需安全換行；主要面板不可產生內部水平 overflow。
- `prefers-reduced-motion: reduce` 下需停用非必要動畫、轉場及平滑捲動，必要狀態不可只靠動畫傳達。
- 匯入需拒絕超過 5 MB、無效 JSON、錯誤根資料、future schema 及不合規巢狀資料。
- 匯入必須先驗證後取代；migration 或 localStorage 寫入失敗時必須 rollback，且不可覆寫目前資料。
- 匯入確認必須使用程式內 dialog；成功、失敗及 quota failure 均需顯示明確狀態。
- 大型 fixture 需保持可 render、無水平 overflow；結果只代表自動化環境，不可代替真實低效能裝置驗證。

## Phase 3.36 驗證重點
- 所有直接式新增面板必須有名稱必填驗證、inline 錯誤及 aria-invalid。
- 已修改草稿離開時必須顯示持久確認；背景點按、關閉及 Escape 不可繞過。
- 編輯既有內容的 Escape 行為需符合該面板保存語意。
- Dialog 必須具有 aria-modal、可讀標題、焦點限制及關閉後焦點返回。
- 草稿 alertdialog 顯示時，後方內容必須 aria-hidden。
- 一般表單及確認控制需至少 44px；代表性輔助面板需通過 320px 溢出檢查。

## Phase 3.35 驗證重點
- 專案及階段新增／編輯必須使用同一套直接式底部面板。
- 面板只可顯示一個 handle；新增使用單剔，編輯關閉時保存並提供刪除。
- 截止日需使用 inline 月曆；階段總預計時間需直接操作。
- 編輯不可搬移父層或清除 milestones、subtasks、完成狀態及任務 pid／mid 關聯。
- 刪除必須繼續使用原有確認與 repairTaskRelations 流程。
- 所有可見控制需至少 44px 並具備可讀名稱。

## Phase 3.34 驗證重點
- 目標新增及編輯必須使用同一套直接式底部面板，不可保留舊式通用 modal 表單。
- 面板只可顯示一個 handle。
- 新增目標右上角使用單剔建立；編輯關閉時保存，右上角提供刪除。
- 目標類型需直接選擇；日期需使用 inline 月曆；顏色控制需至少 44px 並有可讀名稱。
- 編輯不可改動或清除目標狀態、焦點、專案、任務、習慣及其他子資料。
- 所有可見控制需至少 44px 並具備可讀名稱。

## Phase 3.33 驗證重點
- 習慣新增及編輯必須使用同一套直接式底部面板，不可保留舊式通用 modal 表單。
- 面板只可顯示一個 handle。
- 新增習慣右上角使用單剔建立；編輯關閉時保存，右上角提供刪除。
- 重複規則必須保留簡易轉輪與完整自訂兩種模式，以及原有間隔、多日、結束日期及指定次數語意。
- 預設時間必須使用 inline wheel，不可顯示原生 time input。
- 編輯不可清除既有 completionDates、recurrence start 或識別關係。
- 除轉輪內部項目外，所有可見控制需至少 44px 並具備可讀名稱。

## Phase 3.32 驗證重點
- 任務新增及編輯必須使用同一套直接式底部面板，不可保留舊式 h3 加底部按鈕列。
- 面板只可顯示一個 handle。
- 新增任務右上角使用單剔建立；編輯任務關閉時保存，右上角使用刪除操作。
- 截止日使用 inline 月曆；總預計時間及完成次數需直接操作。
- 不可改變任務的目標、專案、階段、截止日、提醒、預計時間或完成次數語意。
- 所有可見任務面板控制需至少 44px 並具備可讀名稱。

## Phase 3.31 驗證重點
- 今日、目標、日程、花園及設定主頁的可見主要控制需至少 44 × 44px。
- 擴大觸控區時不可放大主要文字或破壞原本資訊密度。
- 花園圖示按鈕必須有明確可讀名稱。
- Timeline global FAB 必須繼續隱藏，頁內加入入口必須保留。
- 五頁 audit 必須保持 smallCount 0、unnamedCount 0、沒有水平 overflow 或重複 ID。

## Phase 3.30 驗證重點
- 設定首頁只顯示摘要入口，不可重新堆疊所有設定控制。
- 每週可用時間必須逐日展開，收起時不可建立時間輸入或滾輪。
- 可用時間及回顧時間必須使用具名 inline wheel，不可使用可見原生 time input。
- 專注與休息不可使用可見原生 select。
- 修改開始時間需保留原時長；結束時間不得早於開始時間。
- 設定首頁及每個子頁所有可見控制需至少 44px，且具有可讀名稱。
- 子頁返回只回到設定首頁；設定首頁返回才回到今日。

## Phase 3.29 驗證重點
- Timeline 頁的 `#fab` 必須不可見，但頁內「加入時段」、空檔加入及待安排入口必須保留。
- 今日與目標頁 global FAB 顯示規則不可被改動；花園與更多頁仍維持隱藏。
- UX audit 必須檢查主頁水平 overflow、重複 ID、44px 觸控區、控制可讀名稱及原生表單元件。
- Audit 只提出後續優先項目，不在本輪順手重構其他頁面。

## Phase 3.28 驗證重點
- 每週重複必須顯示具體星期；選擇或明確更改日期後，摘要及 `wday` 必須一致。
- 系列開始／結束日期必須在同一 repeat 展開區及同一面板內完成。
- 指定結束日期必須包含所選日期，翌日起才停止套用。
- 結束日期不得早於開始日期；更改開始日期超過目前結束日期時需同步校正。
- 單日 override 必須載入實際名稱及時間，面板需清楚顯示當日已個別修改。
- 「恢復系列設定」只可刪除目前日期的例外，不得改動系列或其他日期例外。
- 所有重複範圍操作不得重建 `.directsheet` 外殼。

## Phase 3.27 驗證重點
- 直接排程面板頂部只可有一個可見 handle；`.modal::before` 不得與 `.directhandle` 同時顯示。
- 重複修改、重複刪除及草稿放棄確認不得重建 `.directsheet`。
- 顯示確認時只能存在一個 sheet，取消後 DOM identity、草稿及輸入內容必須保持。
- 確認層不可重播底部面板滑入動畫。
- 確認期間背景表單需從輔助技術隱藏，焦點移到確認選項；取消後焦點回到面板操作區。

## Phase 3.25 驗證重點
- 新增專注時段不可回退至原生 select；任務名稱、搜尋及任務選項必須在同一排程面板完成。
- 任務選項需同時提供目標識別與剩餘工時，搜尋獲焦不可令選擇器消失。
- 滾輪需同時支援滑動及點按選項；滑動期間更新選中外觀，停止後才提交資料。
- 滾輪的垂直手勢不可帶動面板或背景頁面。
- 名稱輸入獲焦時收起已展開編輯器；鍵盤完成鍵只收鍵盤，不得儲存或關閉面板。
- 鍵盤、任務切換及滾輪選擇均不得重建 `.directsheet` 外殼。

## Phase 3.24 驗證重點
- 「本次時間」必須可直接點按，不可只是靜態摘要。
- 時長滾輪在同一面板展開，並沿用排程面板的直接編輯排版。
- 修改時長後，結束時間、已安排數字及進度條必須即時一致。
- 展開或操作時長滾輪不得重建面板外殼或重新播放滑入動畫。

## Phase 3.23 驗證重點
- 任務排程面板需顯示本次時間、任務總時間與已安排時間，但不可恢復舊白框表單。
- 本次時間放在日期及起訖時間下面。
- 進度條代表已安排時間 ÷ 任務總時間，並與文字數字一致。
- 新增草稿要計入預覽；編輯既有 Session 不可重複計算原 Session。
- 調整起訖時間時，工時數字及進度條必須局部更新，不重建面板外殼。

## Phase 3.22 驗證重點
- 排程面板不可再依賴「編輯 XX 時段」、欄位小標、提示句或白色卡片框來說明資訊。
- 名稱、日期、起訖時間與重複規則要以字體大小、留白及分隔線自然分層。
- 日期必須在同一面板展開自製月份月曆；時間必須在同一面板以開始／結束切換及共用滾輪完成。
- 修改開始時間時要維持原時長；結束時間不得早於開始時間。
- 關閉、完成與刪除圖示必須為無框 linear style，並保持足夠觸控範圍及對比度。
- 日期、時間及頻率變更不得重建面板外殼或重新播放滑入動畫。
- 各排程流程只顯示必要資料，但需沿用同一排版方向。

## Phase 3.20 驗證重點
- 日程 sheet 的提示小字要收起，不再依賴「列表＋滾輪」或類似說明來教學。
- 一般 modal、篩選與購買面板都應維持同一組 planner-sheet 語言，避免同一產品內出現割裂的面板風格。
- 統一後不能破壞既有新增、刪除、確認與篩選流程。

## Phase 3.19 驗證重點
- 新增面板需維持半屏感，而非回到傳統高表單 modal。
- 新增時必須用右上角單剔完成；直接關閉時要先問繼續修改或刪除草稿。
- 既有時段編輯需偏向自動保存，且右上角顯示垃圾桶。
- 時間設定要以混合式 picker 為主，不再依賴手動輸入。
- 目標選擇要改成較美觀的 bullet list。

## Phase 3.18 驗證重點
- 提起動畫應更輕，不可令卡片一開始就跳得太誇張。
- 放手後要有短暫 settle，而不是立刻硬切到 render 後的最終狀態。
- settle 不可拖太久，亦不可留下殘影或未清理的 drag 視覺。
- 邊緣自動捲動要再保守一點，但仍可在長頁上完成拖拉。

## Phase 3.17 驗證重點
- 吸附仍要存在，但拖拉途中不應有高調的持續提示。
- 拖拉流程應保持單一節奏，不得因長空檔停留而切入另一套微調模式。
- 震動只應出現在關鍵節點，不能每次吸附邊界都反覆觸發。
- direct preview 卡片要比上一版更輕；邊緣自動捲動要更保守。

## Phase 3.16 驗證重點
- 0 分鐘相連任務之間不得出現可見或隱形假空檔。
- 5–29 分鐘空檔必須保留可理解的窄身提示，避免被誤認為完全連續。
- 長空檔要比短空檔有更高的操作面積，但仍維持壓縮式閱讀。
- 拖入 120 分鐘以上長空檔後，停留約 150ms 應顯示局部精細刻度並支援小幅微調。
- 精細模式只輔助控制，不得讓整條時間線 reflow。

## Phase 3.15 驗證重點
- 長按仍為 500ms；觸控提起後卡片在手指上方 28px，滑鼠保留原取點。
- 拖拉卡片本身就是佔位預覽，不建立第二個 occupancy box。
- 卡片時間即時更新，來源卡片維持原時間並保留 ghost。
- 邊界在 10 分鐘內吸附，鎖定後移動超過 15 分鐘才解除。
- 吸附必須有節點、標籤、彈性過渡及可用時短震動；普通五分鐘格線不需要強提示。
- pointer move 以每幀一次更新，時間線及周圍卡片不得 reflow。

## Phase 3.14 驗證重點
- 500ms 長按後才建立 detached floating proxy；提起前超過 8px 視為捲動。
- 浮動卡片從來源原位提起並約 1:1 跟手，來源留下低對比虛線佔位。
- 拖拉時不改來源卡片時間、不展開列、不讓周圍卡片位移。
- 目的地預覽顯示建議 start–end；放開後才寫入 Session。
- 維持五分鐘／相鄰邊界吸附、手動最多兩項重疊及 Task due date 不變。

目標：先用靜態 HTML 驗證 v1 IA 與核心流程，再進入正式 React／TypeScript 開發。

## 1. 文件角色
- `PROJECT.md`：目前原型與已實作邊界，現階段 source of truth
- `FUTURE_ARCHITECTURE.md`：未來正式架構，不代表已完成
- `CHANGELOG.md`：每版實際改動
- `index.html`：唯一程式來源與可操作 IA 原型

## 2. IA Prototype 階段
每次改動依序進行：
1. Analysis：說明問題、假設、受影響流程，不寫 code
2. Plan：列出最小改動及測試方式
3. Confirmation：有歧義時先確認
4. Implementation：只修改已確認範圍
5. Self-check：執行自動與手機版互動檢查
6. Changelog：記錄 Added／Changed／Removed／Fixed

### 現階段重點
優先驗證：
```text
目標 → 今日行動 → 時間安排 → 完成 → 花園回饋
```

暫不追求 production 級：
- React component 架構
- TypeScript types
- PWA／service worker
- 真正系統通知
- 多語言
- 雲端同步

## 3. 靜態原型檢查
```bash
npm run check
python3 -m http.server 4173
# 另一個終端：
npm run smoke:schema7
npm run smoke:timeline
npm run smoke:phase-c
npm run smoke:phase-b
```

手機版至少檢查：
- iPhone 約 390 × 844
- 空白首頁
- FAB 新增流程
- 未有目標時新增任務／專案／習慣
- 建立目標後返回原本新增流程
- 今日、目標、時間軸、花園、更多
- 設定頁原型功能說明
- 每週回顧週次行為
- console／page errors
- 水平 overflow

## 4. 進入正式開發的門檻
以下項目清楚後，才建立 React／TypeScript 專案：
- 底部導航及各頁責任
- 核心資料模型
- 首頁資訊次序
- 新增目標及下一步流程
- 時間安排與任務關係
- 花園回饋規則
- 空白、一般及較多資料狀態
- v1 scope

## 5. 正式開發階段
正式架構建立後才使用：
```bash
npx tsc --noEmit
npm run build
npm test
```
相關方向見 `FUTURE_ARCHITECTURE.md`。

## 6. 版本與交付
- `x.0.0`：架構性改動
- `x.y.0`：新功能或 IA 階段
- `x.y.z`：修 bug／穩定化
- 少量改動可交 diff；需要部署測試時交完整 ZIP


## Phase 3.11C 驗證重點
- 狀態以可掃讀摘要呈現，不使用視覺強烈的膠囊。
- 任務清單只露出一項最高優先工時訊息，完整資訊留在詳情。
- Garden 只保留一層置中 segmented control：花園／圖鑑／造景。
- 更多不再是入口頁；設定、活動歷史和資料備份在同一捲動面板完成。


## Phase 3.12A 驗證重點
- 15 分鐘項目仍可清楚點擊，長項目不會無限制佔據畫面。
- 現在位置與過去未完成項目可一眼識別，但不使用大面積警告色。
- 待安排區預設收起，避免與時間線爭奪注意力。
- 手動第三項重疊必須彈回；既有三項衝突必須保留資料並合併顯示。


## Phase 3.12B 驗證重點
- 普通上下捲動不得在 500ms 前觸發時段拖拉。
- 拖拉期間直接更新卡片內起訖時間，不顯示完整時間刻度。
- 一個手機畫面應能看到多個跨時段安排，長空檔不按真實比例佔位。
- Swipe 只服務頁面級 Segmented Control，並避開任務、日期列、按鈕及表單。


## Phase 3.13 驗證重點
- 非拖拉狀態也應清楚看到工作邊界、任務起訖及空檔中間時間。
- 壓縮空檔的虛線不得令人誤以為列距代表真實時長。
- 長按後局部時間尺必須顯示前後各 90 分鐘及 30 分鐘刻度。
- 卡片位移要接近手指位移，同時保持每 5 分鐘的資料吸附。
- 放開後局部時間尺及展開狀態必須完全清除。
