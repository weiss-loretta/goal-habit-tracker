# PROJECT.md — 目標與習慣（目前原型）

## Phase 3.46 穩定面板整合
- Build P08D-0725A，Schema 7。
- 產品決定：Safari／PWA 不再嘗試隱藏 iOS Input Assistant；優先確保工具列出現時內容不被遮擋、背景連續及版面穩定。

## Phase 3.45 v0.42.4 完整輸入對照
- Build P07D-0725A，Schema 7。
- 目的：確認 Input Assistant 缺席是否依賴 v0.42.4 的整套 textarea、DOM 位置與鍵盤 Sheet 幾何，而非單一屬性。

## Phase 3.44 最小編輯語意診斷
- Build P06D-0725A，Schema 7。
- 移除五種名稱欄的 iOS 欄位導覽候選語意，但保留純文字輸入及可存取名稱。
- 目標是判斷共用輸入提示是否觸發「上一個／下一個／完成」工具列。

## Phase 3.43 純文字名稱欄與簡約所屬位置
- Build P05D-0725A，Schema 7。
- 五種名稱欄使用 plaintext contenteditable，避免 iOS 表單 AutoFill／Input Assistant 語意。
- 任務主面板只顯示目標名稱；完整目標／專案／階段路徑移至第二層。
- 直接隸屬目標的任務顯示「目標 › 零散任務」。

## v0.42.1 目前狀態
- 鎖定外層 `html/body`，App shell 以 visual viewport 高度固定；標題與底部導航不再隨外層文件上下移動。
- 中間內容區保留獨立垂直捲動，支援 iPhone safe area、Safari 分頁及加入主畫面模式。
- 任務與習慣改用 500ms 全卡長按進度；手指移動超過 10px 會取消，避免正常捲動誤觸。
- 長按拖拉期間禁止文字選取並抑制放手後誤開編輯器；習慣清單已接入同一排序機制。
- 任務、習慣、目標、專案及階段編輯器保持穩定主面板，局部設定更新不再令整個面板退出再滑入。
- Modal accessibility 只在首次開啟時取得焦點，避免設定更新及中文輸入期間反覆搶焦點。
- 目標頁 render failure 會顯示可恢復安全狀態，資料仍保留於 localStorage。
- 日程拖拉自動捲動改為操作 App 內頁，而非已鎖定的外層文件。
- Schema 維持 7，storage key 維持 `ght_state_blank`，`index.html` 維持唯一程式來源。

## v0.42.0 目前狀態
- 完成五個主要頁面的 320 × 568、375 × 667、390 × 844、430 × 932 viewport matrix。
- 極窄畫面的頁邊、分段控制及面板間距已調整；長名稱可安全換行，不造成水平 overflow。
- 全域支援 `prefers-reduced-motion: reduce`，必要狀態不依賴動畫傳達。
- 備份匯入拒絕超過 5 MB、無效 JSON、錯誤根資料、future schema 及不合規巢狀資料。
- 匯入先驗證後取代；migration 或 localStorage 寫入失敗會回復匯入前資料。
- 匯入確認改用程式內 dialog；成功、失敗及 localStorage quota failure 均顯示明確狀態。
- 大型 fixture 已覆蓋 20 個目標、100 個專案、500 個階段及 200 個任務。
- Schema 維持 7，storage key 維持 `ght_state_blank`，`index.html` 維持唯一程式來源。

## v0.41.0 目前狀態
- 任務、習慣、目標、專案及階段新增面板統一使用必填名稱驗證。
- 名稱空白時建立操作停用；離開欄位後顯示 inline 錯誤及 aria-invalid。
- 已修改的新草稿按關閉、背景或 Escape 時，顯示持久面板內「繼續修改／捨棄草稿」確認。
- 編輯既有內容按 Escape 時依原面板語意保存，不會直接丟失修改。
- 一般對話框及輔助面板自動取得 dialog、aria-modal 及標題關聯。
- Tab 焦點限制於目前 dialog／alertdialog；關閉後返回原觸發控制。
- 一般表單、批次操作及確認按鈕提升至至少 44px。
- 代表性輔助面板通過 320 × 568 水平溢出檢查。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.40.0 目前狀態
- 專案與階段新增／編輯使用與任務、習慣、目標一致的直接式底部面板。
- 面板只有一個 handle；新增使用右上角單剔，編輯關閉時保存並於右上角提供刪除。
- 專案及階段名稱提升為大型主要輸入，父層目標／專案以平面資訊列顯示。
- 截止日使用 inline 月曆並可清除；階段總預計時間使用直接選項。
- 專案標題現在可直接點按編輯。
- 編輯專案保留所有階段及任務關聯；編輯階段保留完成狀態、子項及任務關聯。
- 刪除專案／階段仍沿用原有確認及任務移回零散任務的安全流程。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.39.0 目前狀態
- 目標新增及編輯使用與任務、習慣一致的直接式底部面板。
- 面板只有一個 handle；新增使用右上角單剔，編輯關閉時保存並於右上角提供刪除。
- 目標名稱提升為大型主要輸入。
- 達成型／持續型改為直接選擇，不再使用原生類型 select。
- 顏色選擇提升至 44px 並加入可讀名稱；目標日期使用 inline 月曆並可清除。
- 「為何而做」改為多行文字區，支援較完整的初心內容。
- 編輯時保留狀態、焦點、專案、任務、習慣及所有子資料。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.38.0 目前狀態
- 習慣新增及編輯使用同一套直接式底部面板視覺語言。
- 面板只有一個 handle；新增使用右上角單剔，編輯關閉時保存並於右上角提供刪除。
- 習慣名稱提升為大型主要輸入；目標、重複規則、每次時間、預設時間及提醒按資訊層級排列。
- 重複規則保留簡易轉輪及完整自訂模式，包括間隔、多選日期、結束日期及指定次數。
- 每次預計時間使用直接選項；預設開始時間改用 inline hour/minute wheel，不再使用原生 time input。
- 編輯時保留原有 recurrence start、完成日期、連續紀錄及日程例外的識別關係。
- 除轉輪內部項目外，習慣面板所有可見控制均達至少 44px。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.37.0 目前狀態
- 任務新增及編輯使用同一套直接式底部面板視覺語言。
- 面板只有一個實體 handle；頂部使用無框線性關閉、建立或刪除操作。
- 任務名稱提升為主要大型輸入，目標／專案／階段、截止日、預計時間、完成次數及提醒按資訊層級排列。
- 截止日使用 inline 月曆；預計時間使用直接選項；完成次數使用 44px stepper，不再依賴數字輸入。
- 新增任務以右上角單剔建立；編輯任務關閉時保存，右上角刪除會進入既有確認流程。
- 保留任務的目標、專案、階段、截止日、提醒、總預計時間及完成次數資料語意。
- 任務面板所有可見按鈕、輸入及選擇控制均達至少 44px。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.36.0 目前狀態
- 今日「焦點／清單」分段按鈕提升至至少 44px。
- 目標頁新增按鈕提升為 44 × 44px。
- 日程「今日／本週」、頁內加入時段及空檔加入按鈕提升至至少 44px；global FAB 仍維持隱藏。
- 花園返回、日夜切換及「花園／圖鑑／造景」分段按鈕提升至至少 44px，並加入明確 aria-label。
- 同步提升週導航、建議完成、目標狀態、階段新增及任務安排等常用控制的觸控高度。
- 五個主頁 UI audit：smallCount 全部為 0、unnamedCount 全部為 0，沒有水平 overflow 或重複 ID。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.35.0 目前狀態
- 設定首頁改為七個摘要入口：每週可用時間、專注與休息、提醒與回顧、顯示與規劃、外部日曆、活動歷史、資料與備份。
- 每週可用時間預設只顯示七日摘要；點按單日才展開開關與開始／結束時間。
- 可用時間及每日回顧時間改用與排程一致的 inline wheel，不再顯示原生 time input。
- 修改可用時間的開始時刻會保留原本可用時長，必要時限制於同一日內。
- 每日最大專注、單次上限及休息時間改為同層選項，不再使用原生 select。
- 設定頁所有可見控制均達至少 44px，且有可讀名稱；UI audit 的設定頁 smallCount 由 32 降至 0、unnamedCount 由 16 降至 0。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.34.1 目前狀態
- Timeline 頁不再顯示 global FAB；今日及目標頁維持原有 global FAB，花園及更多頁仍隱藏。
- Timeline 保留頁內加入入口，不影響「加入時段」、空檔加入及待安排流程。
- 新增 `scripts/audit-ui.mjs`，掃描五個主頁的 overflow、觸控區、可讀名稱、重複 ID 及原生控制。
- 新增 `UX_AUDIT.md`，將後續優化分為 P1–P3；最高優先為設定頁資訊架構、44px 觸控區及時間輸入可讀性。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.34.0 目前狀態
- 每週重複摘要會顯示實際星期，例如「每週一」，不再只顯示「每週」。
- 新選擇每週規則時，以目前面板日期推導星期；明確更改日期時同步更新每週重複日。
- 重複固定時段可在同一 repeat 展開區設定開始日期，以及「永不結束／指定日期」。
- 結束日期採包含當日語意；指定日仍可出現最後一次安排，翌日起停止。
- 已分割系列會直接顯示其 `startsOn`／`endsOn` 有效範圍。
- 目前日期若有 override 或 skip，面板顯示「這一天已個別修改」、實際時間／跳過狀態及「恢復系列設定」。
- 恢復系列設定只刪除目前日期的例外，其他日期及系列規則不受影響。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.33.0 目前狀態
- `.directsheet` 停用舊 `.modal::before` 裝飾 handle，只保留一個實際 `.directhandle`，避免視覺重複。
- 重複時段修改範圍、刪除範圍及新增草稿放棄確認，改為在既有直接排程面板內顯示確認層。
- 確認流程不再替換 `modalRoot` 或建立第二個 sheet，因此不會重新播放面板滑入動畫。
- 取消確認會返回原有表單，保留草稿、面板 DOM identity 及目前編輯內容。
- 確認層加入 alert-dialog 語意、返回按鈕及初始焦點管理。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.31.0 目前狀態
- 新增專注時段不再使用原生任務下拉選單；點按大型任務名稱會在同一面板展開可搜尋任務清單。
- 任務選項顯示目標色點、任務名稱、所屬目標及尚餘時間，選取後沿用同一面板外殼。
- 所有既有滾輪支援點按非中央選項後平滑置中，選取視覺會在捲動期間即時跟隨，停止後再提交數值。
- 滾輪隔離垂直捲動及 overscroll，減少與面板或頁面手勢互相搶奪。
- 名稱及搜尋輸入加入鍵盤協調：名稱獲焦時收起月曆／時間／時長展開內容，鍵盤「完成」只收鍵盤而不關閉面板。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.30.2 目前狀態
- 「本次時間」由純顯示改為可點按操作，位置仍在日期與起訖時間下方。
- 點按後在同一面板展開單欄時長滾輪，沿用 15、30、45、60、90、120、180、240 分鐘選項。
- 調整本次時間會同步更新結束時間、已安排時間及安排進度條。
- 滾輪只局部更新現有面板，不會重建面板外殼或重新播放滑入動畫。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.30.1 目前狀態
- 任務安排、新增專注時段及編輯專注時段補回任務工時摘要。
- 任務名稱下顯示「總計」與「已安排」，並以已安排時間 ÷ 任務總時間顯示幼身進度條。
- 日期與起訖時間下面顯示「本次時間」，位置符合直接編輯式資訊次序。
- 新增安排時，已安排數字與進度條包含目前草稿；編輯既有 Session 時會扣除原時長後再加入新時長，避免重複計算。
- 調整結束時間時，本次時間、已安排時間與進度條會在同一面板即時更新，不觸發整個面板重新滑入。
- Schema 維持 7，storage key 維持 `ght_state_blank`。

## v0.30.0 目前狀態
- 排程相關面板改為直接編輯式 planner sheet，不再以「編輯 XX 時段」標題、欄位小字、提示文案及多個白框建立層級。
- 名稱直接以大型文字呈現；下面依序排列日期、起訖時間與重複規則，主要依字體大小、留白與分隔線建立層級。
- 日期點按後在同一面板展開自製月份月曆，不使用瀏覽器原生日期輸入。
- 時間點按後在同一面板顯示「開始／結束」及共用滾輪；修改開始時間會維持原時長並同步移動結束時間。
- 新增時保留右上角單剔；編輯既有項目保留右上角垃圾桶。圖示改為無框 linear minimal 樣式並跟隨介面文字色。
- 日期、時間、重複及完成類型只更新面板內容，不重建面板外殼，因此不會每次修改都重新播放滑入動畫。
- 同一排版方向已套用到新增時段、任務安排、專注時段與固定時段；各面板只顯示該流程必要資訊。
- Schema 維持 7，storage key 維持 `ght_state_blank`，既有資料與排程語意不變。

## v0.29.1 目前狀態
- 全程式面板視覺統一到同一組 planner-sheet 語言：一般 modal、篩選 sheet、購買 sheet 都沿用相近的圓角、間距與底部升起感。
- 日程時間 picker 的提示小字已移除，例如不再顯示「列表＋滾輪」或「點按以滾輪調整」。
- 既有新增／編輯 sheet 的資訊架構維持不變，但畫面更乾淨，其他面板亦不再明顯跳回舊 modal 風格。
- Schema 維持 7，storage key 維持 `ght_state_blank`，資料模型與排程語意不變。

## v0.28.0 目前狀態
- 日程新增／編輯面板改為半屏 planner sheet，視覺層級由標題、時間、安排方式、重複規則逐段分開。
- 新增流程改成右上角單剔完成；若直接關閉或滑走，會先彈出「繼續修改 / 刪除草稿」警告面板。
- 既有專注時段與固定時段改為偏向自動保存；右上角改為垃圾桶。
- 時間輸入改為混合式：行內顯示 + 常用快捷 + 滾輪；不再以手動輸入為主。
- 完成型新增的目標選擇改成較美觀的 bullet list，不再只是整欄下拉清單。
- Schema 維持 7，storage key 維持 `ght_state_blank`，核心流程與排程語意不變。

## v0.27.0 目前狀態
- 日程拖拉採 Structured 式「提起 → 浮動 → 放置」模型。
- 長按 500ms 才提起；提起前移動超過 8px 取消拖拉並保留捲動。
- 浮動卡片跟隨指標；原時間線維持穩定並保留來源佔位，目的地只顯示小型時間預覽。
- 不再顯示 ±90 分鐘時間尺，不展開目標列，也不移動周圍卡片。
- Session 日期／時間語意、Task 截止日、五分鐘吸附及最多兩項手動重疊規則不變。

## 文件定位
本文件描述目前可執行版本，是現階段技術現況的 source of truth。尚未實作的 React 方向記錄於 `FUTURE_ARCHITECTURE.md`。

## 專案性質
個人生活規劃 App，同時作為 IA、UI/UX 與前端實作練習。核心方向是：透過今日行動推進長期目標，時間安排支援執行，花園提供視覺回饋與情緒價值。

## 目前技術形態
- 單一靜態 `index.html`，包含 HTML、CSS、JavaScript
- localStorage 儲存，key：`ght_state_blank`
- Vercel 靜態部署，無 build step
- `package.json` 只提供專案檢查指令
- 目前不是 React／TypeScript／PWA

## 唯一程式來源
- `index.html` 是唯一可編輯及部署的程式來源。
- 不再保留相同的 `src/index.html`，避免兩份檔案不同步。

## 目前核心物件
```text
Goal
├── Project
│   └── Milestone（階段）
│       └── Task（歸入該階段）
├── Task（零散，pid／mid 為空）
└── Routine / Habit

Task / Milestone
└── Work Session

Timeline
├── Work Session
├── Time Block
└── External Event（只支援資料格式，未連接服務）
```

## 目前主要功能
- 今日頁、花園 Hero 與「為今天選一步」
- 目標、專案、里程碑、任務、習慣
- 目標列表、詳情、安靜狀態摘要與狀態 Sheet
- 今日建議、焦點目標、目標狀態
- 日程內的單一今日時間線與本週安排
- Work Session、Time Block、可用時間與智慧安排原型
- 花園、圖鑑與造景
- 每週規劃、活動歷史、JSON 匯入／匯出
- localStorage schema migration（目前 schema 7）與部分 undo

## 原型功能與邊界
- 提醒選項只保存偏好，不會發送系統通知。
- AI 規劃只屬原型示意。
- 外部日曆只預留資料格式，未連接 Google／Apple／Outlook。
- 多語言與星座獎勵主題尚未實作，介面暫時移除。
- 目前沒有帳戶、雲端同步、PWA、service worker 或離線安裝。

## Phase 3.1 資料規則
- 今日選取與排序按 ISO 日期保存在 `dailyPlans`。
- 習慣以唯一完成日期記錄，同日再次操作會取消打卡。
- 下一步依截止日期排序選出未完成的任務。
- 今日顯示上限包含三步卡，超出項目可手動展開。
- 刪除物件會清理 daily plans、sessions、suggestions 及 block exceptions。
- 花園按週保存 stage／area，週次轉換時自動加入圖鑑。

## Phase 3.2 目標追蹤與次數任務
- 任務可設定「需完成次數」；多於一次時逐次累積並顯示 N／次進度，達標自動完成。
- 已移除目標「等待」狀態、里程磑「等待」與里程磑子任務，里程磑本身即為最小單位。
- 已移除半殘的任務重複（recur），改由次數任務取代。
- 習慣打卡統一經 doAction 處理，overdue 與 streak 由完成日期推算，支援撤銷。
- 匯入備份改為完整取代現有資料（先自動備份，可撤銷）。
- 花園 Hero 顯示本週完成次數與距離下一次成長的提示。
- 目標頁「全部」篩選依狀態分組顯示。

## Phase 3.3 專案任務、進度細算與節奏
- 任務新增 `pid` 欄位：指向所屬專案，為空即為目標下的零散任務。
- 新增／編輯任務時可選擇歸入專案；切換目標會重設專案選擇。
- 目標詳情的專案卡摺疊顯示其任務，專案進度同時計入里程碑與任務。
- 目標進度改為按比例細算：未完成的次數任務依 count／target 折算。
- 目標卡與詳情新增「本週節奏」，以習慣每週完成比例呈現持續型目標的節奏。
- 目標健康度將積壓習慣（overdue routines）納入「需要關注」。
- 新增／編輯目標時領域等同目標名稱（area = title），移除獨立領域欄位與領域篩選。
- 目標狀態與狀態篩選改為收合式選單，減少版面佔用。
- 已知限制：編輯既有任務時尚未提供重新指派專案的介面。

## Phase 3 可用性決策
- 主要觸控目標至少 44 px，checkbox 使用擴展 hit area。
- 動態互動元素支援 Enter／Space，並提供清楚的 focus 樣式。
- Modal 支援 dialog 語意、焦點鎖定、Escape 關閉及焦點返回。
- 支援 iPhone safe-area 與 reduced-motion 偏好。
- 匯入備份加入格式、大小與錯誤狀態檢查。
- 提高次要文字對比度，避免只依賴顏色表達狀態。

## Phase 2 首頁決策
- 花園保留為第一個視覺元素，但壓縮高度。
- 「今天，先走這三步」成為主要行動區，直接連結所推進的目標。
- 主要行動不再於下方清單重複顯示。
- 日程只在首頁顯示下一個安排或今日可用空間摘要。
- 習慣、等待中及即將到期內容降為次要層級；稍後項目預設收起。

## Phase 1 IA 決策
- Roadmap 歸入「目標」，以「目標／路線圖」主分頁切換。
- 「時間軸」改名為「日程」，只保留今日節奏、本週安排與月曆。
- 全域 FAB 只提供任務、習慣與目標。
- 專案只從目標詳情新增。
- 今日完全空白時，以「為今天選一步」連接目標與今日。

## Phase 0 規則
- 沒有目標時新增任務、專案或習慣，先引導建立目標，再返回原流程。
- 每週回顧以週次記錄，每週最多自動彈出一次。
- 介面不得包含 Unicode replacement character `�`。
- 修改後必須執行 `npm run check` 與手機版互動檢查。

## 已知技術債
- 所有邏輯集中在單一 HTML，維護成本高。
- 匯入已有分層資料驗證與交易式 rollback，但尚未涵蓋所有 ID 唯一性、跨物件關聯及日期／分鐘範圍。
- render 與 localStorage persistence 尚未分離；大型真實資料及低效能裝置仍需驗證。
- 自動化已覆蓋主要 dialog 與控制語意，但 VoiceOver、TalkBack、系統級大字及實體裝置返回鍵仍未驗證。
- 正式實作前仍需完成 v1 IA 驗證。

## Phase 3.4 兩層歸屬與里程碑即階段
- 任務歸屬只有兩種：零散（pid 與 mid 皆為空），或某專案某階段（pid＝專案、mid＝里程碑）。
- 里程碑即「階段」，是任務的容器；里程碑進度依其下任務完成度計算，無任務時以里程碑自身完成狀態計。
- 里程碑不再是「可執行／可排程」項目：已從今日、逾期整理、今日候選、自動排程候選與工作 Session 清單移除；僅任務會進入這些流程。
- 目標詳情：專案卡改為以里程碑為分組，任務直接列在所屬里程碑之下；每個里程碑提供「＋ 任務」，專案層提供「＋ 階段 / 里程碑」。
- 新增任務時可選專案→階段；專案若無階段，可就地輸入名稱一鍵建立（甲方案）。切換目標或專案會重設階段選擇。
- 遷移（schemaVersion 6）：既有「掛專案」任務一律轉為零散任務並補 mid 欄位。刪除里程碑或專案時，其下任務會自動退回為零散任務（不會遺失）。
- 編輯既有任務可重新指派專案／階段及設定總預計時間；實際開始時間由專注時段表示（schema 7）。

## Phase 3.5 介面順手化
- 目標狀態篩選：改為狀態藥丸 + 底部彈出選單（filtersheet），移除框內展開的橫向清單。
- 任務卡：整個內容列可點擊開啟編輯（沿用 clickedit），與今日卡片行為一致。
- 編輯任務改為專屬彈窗：可改名稱、重新指派歸屬（零散／專案→階段，支援就地新增階段）、設定截止日、選擇總預計時間（含「未定」），以及需完成次數；儲存時套用兩桶不變式（專案未選階段即回退為零散）。
- 總預計時間：新增／編輯皆改用原生 <select> 下拉，並提供「未定」；未定的任務不強制工時，任務卡顯示「時間未定」。
- 里程碑新增仍沿用共用的時間選擇器（可為「未定」，預設 60 分鐘）。

## Phase 3.6 目標版面簡化與今日清單
- 移除狀態篩選橫框（statusFilterBar 呼叫）；viewGoals 改為依狀態分組：進行中置頂無標題、暫停／已達成為非空才顯示的 sub 分區、封存收入 <details class="donebox"> 摢疊區。statusFilterBar／openStatusSheet 定義保留為死碼，不再被呼叫。
- 今日版面新增 state.todayMode（focus／list）與 setTodayMode；viewToday 頂部渲染「焦點｜清單」切換（沿用 .goalmode 樣式）。
- 新增 todayListView：只取進行中目標下未完成任務，依 tdue 分為逾期（<今日）、今天（=今日）、近幾天（<=今日+upcomingDays，預設 7）、未排期（>視窗的未來日期）、無期限（無截止日）；沿用 itemCard（含目標情境），底部以 .risknote 提醒暫停中目標數量。
- 注：「未排期」目前定義為「有截止日但落在近期視窗之後」；若與預期不同可再調。

## Phase 3.7 狀態卡簡化與排程順手化
- goalStatusControls 移除 .statusctl 白色卡片包裝與「目標狀態」標題，只保留 statuspill 按鈕與下拉選單。
- ④ 排程：openSessionModal 本次時間預設改由 ssDefaultMins 推導（=任務剩餘 unscheduledEffort，上限 maxSession）；新增 ssTaskChanged 在切換任務時重新帶入。
- 新增 quickSchedule：依 freeSlotsFor 掃描未來 14 日，將任務剩餘時間排進第一個≥本次需要且≥十五分的空檔（受 maxSession 限），寫入 state.sessions（auto:true），logEvent('session_scheduled')，支援復原。任務卡新增「＋排入」按鈕觸發。
- 非自動排程；之後可加一個預設關閉的自動排程設定（待辦）。

## Phase 3.8 狀態標籤合一
- goalStatusControls 改為渲染一顆可點的狀態藥丸（沿用 .tag.status.<key> 顏色 + ▾ 暗示），內含下拉選單；以 event.stopPropagation 避免覇發父層點擊。
- 詳情頭 dhead 原本只顯示的靜態狀態 span 改用 goalStatusControls；移除 goalpulse 下方重複的狀態控制區。狀態全域只顯示一處。
- 選單項目 min-height 40px、藥丸 min-height 26px，確保手機觸控區。.dhead／.meta 無 overflow:hidden，選單不被裁切。

## Phase 3.9 花園花種系統
- 新增 weekSpeciesKey()：依本週（ISO 週起）事件判定花種，優先序 legend(milestone_done) > bloom(n>=8) > vivid(不同目標>=3) > steady(不同日子>=4) > solace(n<=2) > daily。門檻為可調常數。
- plantSVG 新增第五參數 sp（花種）；solace 提前回傳苔丘，其餘於盛開（stage 3）呼叫新增 bloomSVG(sp,topY,c) 畫出對應花型（均為原本平面圓/橢圓畫法）。
- ensureGardenWeek 將 weekSpecies 寫入並於週滾動時連花種存入 collection；collectionHTML 依每週 species 渲染並新增 speciesDexHTML() 六種圖鑑（已收集/鎖定）。
- gardenBanner 與 islandSVG 的現役花都傳入 weekSpeciesKey()。領域相關機制本次未動（待後續收尾）。
## Phase 3.10A schema v7 核心資料模型
- Task 的截止日與實際安排分離：Task 不再保存開始時間；Work Session 是唯一實際日程記錄。
- schema 6 的 `due + time` 會轉成 migrated Session；工時未定的舊資料保留 60 分鐘舊版顯示並標記 `needsReview`。
- 有任務的階段由子任務狀態推導完成；Goal／Project／Milestone 使用同一套進度聚合，避免重複計算。
- 關聯修復成為 render／migration 不變式；失效 `pid`／`mid` 自動清空。刪除專案或階段時任務退回零散任務。
- DailyPlan 保留過去計劃歷史；完成任務只移除今日及未來計劃引用。
- ProgressEvent 以 `sourceKey` 去重並支援 reversal，避免完成→重開→再完成重複計算花園進度。
- 水滴改用 RewardTransaction 帳本；已購道具不因任務重開／刪除而令餘額變負數。
- Habit recurrence 新增 `startsOn`、`endsOn`、`maxOccurrences`；預設時段與單日例外留待 Phase 3.10B 接上時間線 UI。
- GardenWeek 只在跨週後成為 finalized collection；本週品種不再提前計作已收集。花園主色由本週主要推進目標的 `goal.color` 決定。
- 領域欄位只為舊 UI／備份相容暫留；已退出花園核心邏輯，完整 UI 與篩選移除安排於 Phase 3.10C。
## Phase 3.10B 人性化日程時間線
- 今日日程採簡潔摘要、展開刻度及長按拖拉三種狀態；安排或拖拉完成後自動收回。
- 任務旁 `＋` 改為手動安排入口；時間線可搜尋所有仍有未安排工時的任務，亦可新增任務後續接安排。
- 本次安排時長獨立於 Task 總預計；空檔不足、重疊或超出可用時間時禁止放置。
- 專注時段以 15 分鐘吸附拖拉；完成時只記錄投入時間，達到總預計後再詢問是否完成 Task。
- Habit 可設定 `estimateMinutes` 與 `defaultSchedule`；單日移動寫入 `habitScheduleExceptions`。
- 日程只顯示今日／本週，月曆 UI 已移除。Phase 3.10C 才處理底部花園、路線圖、任務視覺層級、狀態 Sheet、FAB、領域 UI 與全介面中文。
## Phase 3.10C 導航與互動收斂
- 底部導航固定為今日／目標／日程；花園由今日 Hero 進入。
- 主要導航會重設到標準入口，避免返回上次過深的子頁。
- 目標頁不再顯示路線圖及領域篩選；舊 `area` 欄位只保留作資料相容，不再驅動主要 UI 或新資料。
- 全域 FAB 直接新增行動，可切換成習慣；新增目標只在目標頁。
- 目標狀態使用底部 Sheet；階段內任務改成從屬小列，永久拖曳把手已隱藏。
- 每週規劃移到今日；任務建議及稍後項目名稱均可開啟詳情。
- Phase 3.10 A／B／C 的資料模型、時間線與 IA 收斂已完成。
## Phase 3.11A 單一 Structured 時間線
- 今日只保留一條時間線；工作時間與實際時段邊界構成主要刻度，拖拉時才顯示 15 分鐘輔助線。
- 空檔現改為三層語意：0 分鐘直接貼合、5–29 分鐘窄身提示、30 分鐘以上分級壓縮並保留操作面積。
- 重疊及容量超限不再阻擋，時間線以並排時段與警告圖示呈現。
- 新增時段以「需要完成／打卡」及 recurrence 推導底層 Session、Habit 或 TimeBlock。
- Habit 與重複 TimeBlock 的拖拉支援單日例外及今天以後的預設更新。
- Phase 3.11B 將處理今日、目標／階段及本週的全域任務排序與跨日拖拉。
## Phase 3.11B 全域直接操作
- 今日任務拖拉只更新當日 `DailyPlan.order`。
- 目標任務只可在同一階段或同一零散任務容器內排序；截止日為第一排序鍵，同日期／無日期才接受手動順序。
- 本週拖拉已安排 Session 時只改 Session 日期並保留開始時間；拖未安排 Task 時建立 Session，不改 Task due date。
- 所有新拖拉採長按拾起、視覺浮起、投放高亮及無效位置彈回。
- Phase 3.11C 將集中處理目標摘要、工時資訊、花園「花園／圖鑑／造景」及單一設定面板。


## Phase 3.11C 狀態與次要 IA 收斂
- 目標狀態使用安靜摘要列，點按後仍開啟同一個狀態 Sheet。
- 任務列依逾期、警告、未安排、已安排、超出預計及時間未定的優先順序顯示單一工時摘要。
- 花園內層導航固定為「花園／圖鑑／造景」，不與全域底部導航競爭。
- 右上角選單直接進入單一設定面板；活動歷史與資料備份不再使用中介入口頁。

## Phase 3.12A 時間線閱讀與執行狀態
- 時段卡採半比例高度：15／30／60／90／120 分鐘分別約 44／52／72／92／112 px，之後以較慢比例延伸。
- 今日時間線標示現在、進行中、已完成與過去未完成，進入時自動捲到現在附近。
- 待安排任務區置於時間線末端並預設收起；拖入時間線只建立 Session，不改 Task 截止日。
- 手動操作最多同時兩項；第三項會被阻止。系統或外部資料造成的三項以上衝突則合併顯示並完整保留。

## Phase 3.12B 壓縮日程與手勢
- 今日日程不再以完整等比例時間軸呈現；安排依時間排序，連續任務直接貼合，短空檔保留窄身提示，較長空檔壓縮為分級操作列。
- 任務卡高度分為 48／56／68／80 px 四層，只傳達概略時長。
- 工作開始與結束固定顯示生活化文案，但不建立可完成的資料項目。
- 時段拖拉需長按 500ms；成立前移動超過 8px 會交還頁面捲動。拖拉每 8px 改變 5 分鐘，卡片內時間即時更新。
- 真正的頁面分段可水平 Swipe；切換後回到頁頂，表單、橫向日期列及任務拖拉不觸發。

## Phase 3.13 常駐彈性時間線
- 壓縮行程左側一直顯示簡約時間線，但列距不代表全日真實比例。
- 工作開始／結束及任務起訖時間使用邊界標記；連續任務共用邊界，短空檔顯示剩餘時間，長空檔保留中央時間與較高操作面積。
- 卡片內仍保留完整起訖時間。相鄰安排可共用同一邊界標籤。
- 長按 500ms 後顯示前後各 90 分鐘的局部時間尺，每 30 分鐘一格；卡片 1:1 跟手移動，時間每 5 分鐘更新。
- 目標區域局部展開並讓周圍項目讓位；靠近視窗邊緣時自動捲動。


## Phase 3.41 固定編輯面板與本週垂直日期 Rail
- 編輯器採固定頂部 anchor；鍵盤彈出只改變底部可見高度，不再把整張面板推到畫面頂部。
- Handle、頂部操作與名稱維持可見；詳細資料使用獨立 scroll container。
- 底部導航採 56px 本體與較多上方留白，同時保留完整 `safe-area-inset-bottom`。
- 本週移除頂部橫向 drop bar；拖拉時顯示右側垂直七日 Rail，並同步高亮真正的垂直日期區塊。
- 使用者可直接拖到可見日期區塊，或利用 Rail 跳到畫面外日期；停留及邊緣拖拉會協助捲動。
- Phase 3.41 不修改 Schema 7、`ght_state_blank` 或 Task／Session 日期語意。


## Phase 3.42 iOS 名稱欄回歸修正
- 五種編輯器的名稱 textarea 從詳細資料捲動容器移至固定名稱區。
- textarea 屬性與 v0.42.4 保持一致，不改動中文輸入、AutoFill 或 Enter 行為。
- 鍵盤可視邊界下方以獨立背景 Bridge 覆蓋 Input Assistant 區域，不讓內容區延伸到鍵盤下方。
- 本修正不改動固定 Sheet anchor、截止日期、本週垂直日期 Rail、Schema 7 或儲存鍵。
